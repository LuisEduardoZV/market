import { useQuery } from 'react-query'

import { Flex } from 'antd'

import Banner from '../ui-components/Banner'

const Home = () => {
  return (
    <Flex>
      <Banner />
    </Flex>
  )
}

export default Home
